name: Docker Test

on:
  push:
    branches: [ main ]
  workflow_dispatch:    # ← Important pour le bouton manuel!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Test Docker
        run: |
          docker --version
          docker run hello-world
